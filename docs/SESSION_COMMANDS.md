# Session Commands — 2026-05-15

## Commands Given

1. **explain the application**

2. **run via docker**

3. **identify 2 main features**

4. **create a good details functional flow of the app**

5. **save tis all in an md file**

6. **generate a test procedure based on the md file and the code structure for create card and drag and drop card**

7. **follow the pattern for creating the test procedure, give a description of the functionality/feature. Next under a heading talk about the test plan, next talk about all smoke, unit, integration, end to end test and where can you find those test inside the project structure, next mention dependencies for the project (database, queue), next create a table with the test cases, test steps, test result and then a column for is automated and then classify it as smoke, end to end, unit**

8. **for the test procedure let us start with the first section — Author, Created On, Document Status, Target Release, Epic / Jira Link, Reviewers**

9. **now add the overview, add all the heading leave them empty if we don't have the data — Overview (What the feature/service does, Key user workflows, Why this feature exists) and References (Functional spec, Architecture doc, API contract)**

10. **use the CREATE_TEST_PROCEDURE.MD to rewrite the TEST_PROCEDURE_CARDS.md and also create a text or another readable doc**

11. **Update create test procedure skill file to also add the kind of tests being added from functional, security, authentication, authorization so that will be another column in the table**

12. **rerun the skill on the txt file, according to the md file test steps and expected results should all be in one table with ID, test case, type and Automated**

13. **create a md file of all the commands I gave you today**
